# Copy this to docker-compose.env and set your own values.
# docker-compose.env is gitignored — never commit real passwords.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_use_a_strong_random_password
