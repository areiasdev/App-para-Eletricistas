# Copy to docker-compose.prod.env and fill in real values.
# docker-compose.prod.env is gitignored — never commit secrets.

# ── Database ──────────────────────────────────────────────────────────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_strong_random_password

# ── JWT (minimum 32 chars, generated randomly) ───────────────────────────────
JWT_SECRET=change_me_use_openssl_rand_hex_32
JWT_ISSUER=https://tecnicoapp.pt
JWT_AUDIENCE=https://tecnicoapp.pt
JWT_EXPIRY_MINUTES=60
JWT_REFRESH_TOKEN_DAYS=30

# ── URLs ──────────────────────────────────────────────────────────────────────
# Public URL where the frontend is hosted (used in CORS and password-reset emails)
FRONTEND_URL=https://app.tecnicoapp.pt
# Public URL of the API (baked into the Next.js bundle at build time)
NEXT_PUBLIC_API_URL=https://api.tecnicoapp.pt

# ── App behaviour (all optional — defaults shown) ─────────────────────────────
# Name on login screens, system emails and the browser tab. Client-facing emails and PDFs
# use the company name set in Perfil instead.
APP_PRODUCT_NAME=TécnicoApp
# Local clock for scheduled reminders (IANA id)
APP_TIME_ZONE=Europe/Lisbon
# Due date of a new invoice = issue date + N days
INVOICE_PAYMENT_TERM_DAYS=30
# false = only the first account can self-register; everyone else joins by team invite
ALLOW_OPEN_REGISTRATION=false

# ── SMTP (leave blank in dev — emails are logged to console instead) ──────────
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USERNAME=postmaster@mg.tecnicoapp.pt
SMTP_PASSWORD=change_me
# Defaults to APP_PRODUCT_NAME when blank
SMTP_FROM_NAME=
SMTP_FROM_ADDRESS=noreply@tecnicoapp.pt

# ── Stripe (this install's own account — collects invoice payments from this ──
# trade company's own customers. Card + MB WAY. Leave blank to disable online
# payment on invoices; the rest of the app works fine without it.)
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PUBLISHABLE_KEY=pk_live_...

# ── Twilio (WhatsApp/SMS notifications — leave blank to disable; quotes/ ──────
# invoices/reminders still work, notifications are just skipped and logged)
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=change_me
TWILIO_FROM_NUMBER=+1...
TWILIO_WHATSAPP_FROM_NUMBER=+1...
